package mps.demo.calc.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class DataType_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<DataType> constants = ListSequence.fromList(DataType.getConstants()).iterator();
    while (constants.hasNext()) {
      DataType constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<DataType> constants = ListSequence.fromList(DataType.getConstants()).iterator();
    while (constants.hasNext()) {
      DataType constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    DataType constant = DataType.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
